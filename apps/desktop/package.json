{
  "name": "liblog-desktop",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix liblog-desktop",
    "build": "npm run build --prefix liblog-desktop",
    "preview": "npm run preview --prefix liblog-desktop"
  }
}
